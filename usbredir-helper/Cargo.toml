[package]
name = "usbredir-helper"
version = "0.1.0"
edition = "2018"

[dependencies]
zbus = { git = "https://gitlab.freedesktop.org/dbus/zbus" }
zbus_polkit = { git = "https://gitlab.freedesktop.org/dbus/zbus" }
async-std = { version = "1.9.0", features = [ "attributes" ] }
